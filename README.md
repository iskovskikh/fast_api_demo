
# FastApi DDD Demo Chat

Проект чата на FastApi + MongoDB с использованием подходов DDD.

Д   оступные команды:

`make all`

`make app`

`make app-down`

`make app-logs`

`make app-shell`

`make storages`

`make storages-down`
